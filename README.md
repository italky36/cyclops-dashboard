# Cyclops Dashboard

Веб-интерфейс для управления номинальным счётом через Cyclops API банка Точка.

![Dashboard Preview](./docs/preview.png)

## Возможности

- 🔄 **Переключение между слоями** (PRE/PROD)
- 👥 **Управление бенефициарами** (ЮЛ, ИП, ФЛ)
- 💳 **Виртуальные счета** — создание, просмотр балансов, переводы
- 📄 **Сделки** — создание и исполнение выплат
- 💰 **Платежи** — идентификация входящих платежей
- ⚙️ **Настройки** — управление ключами, автоматизация

## Безопасность

### Хранение ключей

⚠️ **Приватные ключи хранятся только на сервере** в переменных окружения. Они никогда не отправляются на клиент.

Архитектура безопасности:
```
[Браузер] → [Next.js API Routes] → [Cyclops API]
              ↓
         Приватный ключ
         (env variables)
```

### Рекомендации

1. **Никогда не коммитьте** файл `.env.local` в репозиторий
2. Используйте **разные ключи** для PRE и PROD
3. В production используйте **secrets management** (HashiCorp Vault, AWS Secrets Manager)
4. Настройте **IP whitelist** на стороне Cyclops
5. Включите **HTTPS** в production

## Быстрый старт

### 1. Клонирование и установка

```bash
git clone <repository>
cd cyclops-dashboard
npm install
```

### 2. Настройка окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env.local

# Отредактируйте .env.local и добавьте ваши ключи
```

### 2.1 Авторизация (MVP)

При первом запуске откройте `/login` и создайте администратора. Данные пользователя
сохраняются в SQLite файле, путь задается через `SQLITE_PATH`.

Обязательно задайте `AUTH_SECRET` в `.env.local`:
```
AUTH_SECRET="<случайная-строка>"
SQLITE_PATH="./data/cyclops.db"
```

### 3. Генерация ключей (если нужно)

```bash
# Генерация RSA ключей
openssl genpkey -algorithm RSA -out private_key.pem -pkeyopt rsa_keygen_bits:4096
openssl rsa -pubout -in private_key.pem -out public_key.pem

# Публичный ключ отправьте в техподдержку Cyclops
# Приватный ключ добавьте в .env.local
```

### 4. Запуск

```bash
# Режим разработки
npm run dev

# Production сборка
npm run build
npm start
```

Откройте [http://localhost:3000](http://localhost:3000)

## Структура проекта

```
cyclops-dashboard/
├── app/
│   ├── api/
│   │   ├── cyclops/          # API для запросов к Cyclops
│   │   └── settings/         # API настроек
│   ├── (dashboard)/
│   │   ├── beneficiaries/    # Страницы бенефициаров
│   │   ├── virtual-accounts/ # Виртуальные счета
│   │   ├── deals/            # Сделки
│   │   ├── payments/         # Платежи
│   │   └── settings/         # Настройки
│   ├── layout.tsx
│   ├── page.tsx              # Главная страница
│   └── globals.css
├── components/
│   ├── ui/                   # UI компоненты
│   ├── forms/                # Формы
│   └── layout/               # Layout компоненты
├── hooks/
│   └── useCyclops.ts         # Хук для работы с API
├── lib/
│   ├── cyclops-client.ts     # Клиент Cyclops API
│   ├── crypto.ts             # Криптографические утилиты
│   └── store.ts              # Zustand store
├── types/
│   └── cyclops.ts            # TypeScript типы
└── .env.example
```

## API Cyclops

### Эндпоинты

| Слой | URL |
|------|-----|
| PRE (тест) | `https://pre.tochka.com/api/v1/cyclops` |
| PROD | `https://api.tochka.com/api/v1/cyclops` |

### Основные методы

#### Бенефициары
- `create_beneficiary_ul` — создание ЮЛ
- `create_beneficiary_ip` — создание ИП
- `create_beneficiary_fl` — создание ФЛ
- `list_beneficiary` — список
- `activate_beneficiary` / `deactivate_beneficiary`

Примечание: регистрация бенефициара в мастер-системе асинхронная.
Поле `is_added_to_ms` обновляется через `get_beneficiary`; проверка статуса
разрешена не чаще 1 раза в 5 минут на одного бенефициара.

#### Виртуальные счета
- `create_virtual_account` — создание
- `list_virtual_account` — список
- `get_virtual_account` — детали
- `transfer_between_virtual_accounts` — перевод

#### Сделки
- `create_deal` — создание
- `execute_deal` — исполнение
- `rejected_deal` — отмена

#### Платежи
- `list_payments_v2` — список
- `identification_payment` — идентификация

## Типы выплат

| Тип | Описание |
|-----|----------|
| `payment_contract` | По реквизитам (р/с) |
| `payment_contract_by_sbp` | Через СБП |
| `payment_contract_to_card` | На карту |
| `commission` | Комиссия площадки |
| `ndfl` | Налог в бюджет |

## Регламент платежей

### По реквизитам
- Будни: 01:10 — 19:50 МСК
- Воскресенья: 11:10 — 16:50 МСК (если банк-получатель работает)

### СБП и карты
- 24/7
- СБП: до 5 минут
- Карты: до 2 минут (макс. 1 час)

### Внутри Точки
- 24/7, мгновенно
